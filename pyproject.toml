[project]
name = "binaural-sound-lab"
version = "0.1.0"
description = "A simple sandbox for binaural-style stereo sound experiments."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "sounddevice",
]
